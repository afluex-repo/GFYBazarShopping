@model GfyBazar.Models.Master

@{
    ViewBag.Title = "BannerList";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}




@using (Html.BeginForm("BannerList", "Master", FormMethod.Post, new { @id = "form1" }))
{

    <p style="color: red">
        @TempData["BannerList"]
    </p>
    <div class="card">
      
            @*<div class="row">
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Banner Name <span style="color: red">*</span></label>
                        @Html.TextBoxFor(m => m.BannerName, new { @class = "form-control", @Placeholder = "Offer Name" })
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="form-group">
                        <br />
                        <input class="btn btn-success" type="submit" value="Search" id="btnSearch" name="Search" />
                    </div>
                </div>
            </div>*@
        
    </div>
        <br />
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Banner List</h4>
                    <div class="row">
                        <div class="col-12">
                            <div class="table-responsive">
                                <table id="example23" class="display nowrap table table-hover table-striped table-bordered dataTable no-footer">
                                    <thead>
                                        <tr>
                                            <th hidden>PK_GalleryID</th>
                                            <th>S No.</th>


                                            <th>Image</th>
                                            <th>Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @if (Model != null)
                                        {
                                            if (Model.lstProduct != null)
                                            {
                                                int i = 1;
                                                foreach (var Item in Model.lstProduct)
                                                {
                                                    <tr class="text-left">
                                                        <td hidden>@Item.BannerID</td>
                                                        <td>@i</td>


                                                        <td> <img src="@Item.BannerImage" id="img" height="100" width="100" /></td>

                                                        <td>
                                                            <a href="/Master/DeleteBanner?id=@Item.BannerID" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this item?');">Delete</a>
                                                        </td>
                                                    </tr>
                                                    i++;
                                                }
                                            }
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
}


